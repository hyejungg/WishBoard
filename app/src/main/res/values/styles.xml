<resources>
    <!--v23이하에서 적용-->
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/realBlack</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:includeFontPadding">false</item>
    </style>
    <style name="AppThemePopup" parent="AppTheme">
        <!-- @param : windowFullscreen : 상태바 없애기(false)-->
        <item name="android:windowFullscreen">false</item>
        <!--@param : backgroundDimEnabled : 반투명 배경 적용-->
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowBackground">@color/semiTransparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowAnimationStyle">@null</item>
        <!--@param : windowSoftInputMode : 소프트 키보드에 가려지지 않도록(adjsutPan), 기본 값(stateUnspecified) -->
        <item name="android:windowSoftInputMode">stateUnspecified|adjustPan</item>
    </style>

    <style name="AppTheme.NumberPicker">
        <item name="android:textSize">15dp</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:fontFamily">@font/nanum_square_b</item>
    </style>

    <!-- @param : MoreFolderDiolog에서 사용될 하단으로 올라오는 diolog style @TODO : 적용 안되는 문제 발생 -->
    <style name="CustomBottomSheetDialogTheme" parent="Theme.MaterialComponents.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/CustomBottomSheetStyle</item>
    </style>
    <style name="CustomBottomSheetStyle" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@android:color/transparent</item>
    </style>
</resources>